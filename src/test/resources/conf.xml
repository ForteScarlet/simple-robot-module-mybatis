<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- 引用db.properties配置文件 -->
    <properties resource="db.properties"/>
    <!--
    environments:开发模式
    work:工作模式
    default="development"，id="development"，两个的属性值必须一致
     -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <!-- value属性值引用db.properties配置文件中配置的值 -->
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${name}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>

    <!-- 在配置文件中 关联包下的 接口类-->
    <mappers>
        <!--<mapper class="com.forte.test.http.util.mapper.CaliburImageHomesMapper"/>-->
        <!--<mapper class="com.forte.test.http.util.mapper.CaliburImageMapper"/>-->
        <!--<mapper class="com.forte.test.http.util.mapper.BilibiliDrowImageHomeMapper"/>-->
        <!--<mapper class="com.forte.test.http.util.mapper.BilibiliDrowImageMapper"/>-->
        <!--<mapper class="com.forte.test.http.util.mapper.MsgMapper"/>-->
        <package name="com.forte.mapper" />
    </mappers>
</configuration>